pipeline {
    agent any
    
    stages {
        stage ('Compile Stage') {

            echo 'Compile Repo'
        }

        stage ('Testing Stage') {

            echo 'Testing Stage'
        }

        stage ('Deploy') {

            echo 'Deploy Repo'

         }
    }
}